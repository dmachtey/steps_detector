version: "1.0.0"

dependencies:
  # Dependencia del sistema (Core) unificada
  idf: ">=5.0.5"

  # Dependencias de Espressif (Hardware y DSP)
  espressif/esp_io_expander_tca9554: "^1.0.1"
  espressif/es8311: "^1.0.0"
  espressif/esp-dsp: "^1.4.9"  # <-- ¡Aquí está la librería para el audio!

  # Dependencias de UI / Pantalla
  lvgl/lvgl:
    version: "8.4.*"
    public: true

  esp_lcd_touch_ft5x06:
    version: "*"
    public: true

  # Dependencias Locales
  esp_lcd_sh8601:
    path: "../components/esp_lcd_sh8601"
    public: true